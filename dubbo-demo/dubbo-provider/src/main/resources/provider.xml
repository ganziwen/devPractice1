<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">


    <!--提供方应用名，用于在管理工具中区分是哪个提供方，用于计算依赖关系-->
    <dubbo:application name="hello-world-app"/>

    <!--使用multicast广播注册中心暴露服务地址    -->
    <!--    <dubbo:registry address="multicast://224.5.6.7:1234"/>-->
    <!--推荐使用zookeeper来进行注册发现,用 zookeeper 开头    -->
    <dubbo:registry address="zookeeper://www.xiaowenshu.cn:2181"/>
    <!--    下面的写法同上-->
    <!--    <dubbo:registry protocol="zookeeper" address="www.xiaowenshu.cn:2181"/>-->

    <!--用dubbo协议在20880端口暴露服务    -->
    <dubbo:protocol name="dubbo" port="20881"/>

    <!--声明需要暴露的服务接口   只能注册接口，引用实现类 -->
    <dubbo:service interface="service.DemoService" ref="demoService" timeout="1000" retries="5"/>

    <!--和本地bean一样实现服务-->
    <bean id="demoService" class="service.impl.DemoServiceImpl"></bean>
</beans>
